# 📒 iOS 极简记账 App (MVP) 产品需求文档

## 📄 修订历史
| 版本号 | 修订日期 | 修订人 | 修订内容 | 状态 |
| :--- | :--- | :--- | :--- | :--- |
| v1.0 | 2026-05-18 | 小马 / AI | MVP 初始版本功能定义 | 草案 |

---

## 🎯 1. 产品概述
*   **产品名称**：未定（暂称：极简账本）
*   **产品定位**：一款纯本地、无广告、无社交的极简个人记账与预算管理工具。
*   **核心理念**：通过极简的交互降低记账门槛，通过即时的视觉告警帮助用户克制消费。
*   **目标用户**：希望看清资金流向、有月度省钱/控费需求，同时极度重视个人财务隐私的用户。

---

## 👥 2. 用户角色与典型场景

### 场景 A：快速记账（追求速度）
> **用户行为**：用户在超市买完日用品结账出来。掏出手机打开 App，希望在 3 秒内完成记录并锁屏，不希望被任何开屏广告、弹窗或复杂的理财推荐干扰。

### 场景 B：消费越界预警（追求控制）
> **用户行为**：月中，用户想买一款新出的游戏机。打开 App 准备记账时，首页的预算进度条已经变成了橙量黄色。看了一眼告警，用户决定把游戏机的购买计划推迟到下个月。

---

## 🛠️ 3. 核心功能详细需求

### 3.1 记账模块（Core Ledger）

#### 3.1.1 记账入口
*   **触发条件**：点击首页全局悬浮的 `+` 按钮。
*   **交互形式**：从屏幕底部向上滑出（Modal）半屏记账面板。

#### 3.1.2 记账信息输入
*   **类型切换**：顶部提供「支出」与「收入」分栏切换（Segment Control），默认停留在「支出」。
*   **金额输入**：采用 **App 内置数字键盘**（非系统自带键盘，包含 0-9、删除键、小数点、确认键），确保界面整体感。
*   **分类选择**：提供一页式图标面板，用户点击图标即选中。
*   **选填项**：
    *   **备注**：点击后唤起系统键盘，支持输入单行文字（限 20 字）。
    *   **日期**：默认当天，点击可呼出系统滚动选择器（DatePicker）选择过去的时间。

#### 3.1.3 预设分类规范
*   **支出分类（5个）**：🏠 房贷/房租、🛍️ 日用、🎮 娱乐、🍔 餐饮、🚗 出行。
*   **收入分类（3个）**：💰 工资、📈 投资、🎁 其他。
*   *注：MVP 阶段不支持分类的自定义、删除或排序。*

---

### 3.2 月度预算与告警模块（Budget & Alert）

#### 3.2.1 预算设置
*   **入口**：设置页面 ->「每月预算管理」。
*   **逻辑**：提供一个开关。开启后，用户可输入一个正整数作为当月总预算值（人民币元）。

#### 3.2.2 首页预算进度条（核心看板）
*   **展现公式**：
    $$\text{预算消耗率} = \left( \frac{\text{当月累计支出}}{\text{当月总预算}} \right) \times 100\%$$
*   **状态机与视觉表现**：

| 状态 | 触发条件 | 进度条颜色 | 界面文案/表现 |
| :--- | :--- | :--- | :--- |
| 🟢 **安全** | 消耗率 < 80% | 绿色 | 正常显示当前已花与剩余额度。 |
| 🟡 **预警** | 80% ≤ 消耗率 < 100% | 黄色 | **首次达到该状态时**，用户打开 App 触发一次轻量全局弹窗提示："本月预算已消耗超过 80%，请注意控制娱乐和日用支出哦~" |
| 🔴 **超支** | 消耗率 ≥ 100% | 红色 | 进度条持续高亮红色。每次打开 App，首页顶部常驻红字风险提示："本月预算已超支！" |

---

### 3.3 数据统计与历史流水（Analytics & History）

#### 3.3.1 首页历史流水列表
*   **排序**：按日期倒序排列（今天、昨天、前天……）。
*   **聚合**：同一天的账目聚合在同一个日期外壳下，并在右侧小字显示"当日总支出"和"当日总收入"。
*   **单笔账目展示**：分类图标 + 分类名称 + 备注（如有）+ 金额（支出显示为黑色 `-¥XX`，收入显示为绿色 `+¥XX`）。
*   **手势操作**：支持对单笔账目**向左滑动删除**，删除后实时重新计算当月预算和流水。

#### 3.3.2 月度统计看板
*   **时间维度**：按月切换（默认展示当前自然月）。
*   **核心指标**：总收入（元）、总支出（元）、结余（元）。
*   **图表分析**：一个极简的**分类支出排行榜**（按金额从大到小降序排列），以进度条比例展现某分类占总支出的百分比。

---

## 📱 4. 页面结构与原型逻辑 (Information Architecture)

App 采用最符合 iOS 直觉的 **双 Tab 标签页** 结构：
[ App 根视图 ]
├── Tab 1: 【明细】(首页)
│     ├── 顶部：当前月份选择、本月总收支结余数据
│     ├── 中部：月度预算进度条（动态变色）
│     ├── 下半部：每日流水列表（支持左滑删除）
│     └── 悬浮：[ + ] 记账大按钮 -> 弹出半屏记账页
│
└── Tab 2: 【统计与设置】
├── 上半部：本月分类支出排行榜
└── 下半部：设置区域 -> [ 每月预算开关与金额设置 ]
---

## 🚫 5. 非功能性需求（MVP 边界约束）

为了保证 MVP 阶段能够快速交付，以下功能明确**不放入**本版本中：
1.  ❌ **不引入任何网络请求**：没有注册登录，没有云端同步，数据 100% 存在手机本地。
2.  ❌ **不支持多账本**：有且仅有一个默认个人账本。
3.  ❌ **不支持自定义分类**：用户不能新增或修改"日用"、"娱乐"等分类。
4.  ❌ **不支持图片或拍照记账**：仅支持纯文本和数字。

---

## ✨ 6. 细腻体验设计建议（UX Details）

*   **震动马达反馈（Haptics）**：
    *   在内置键盘点击数字时，触发系统最轻微的 `light` 触觉反馈。
    *   点击"保存成功"时，触发 `success` 成功反馈。
    *   当预算进入**红色超支**状态、用户再次记账时，保存成功后追加一次 `warning` 警示震动。
*   **空状态设计（Empty State）**：
    *   月初或者第一次打开 App、没有任何流水时，首页列表不要留白，展示一个可爱的存钱罐插画，并文案提示："本月还没有账目哦，点击下方 + 号记一笔吧！"